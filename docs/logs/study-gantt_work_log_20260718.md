# Study Gantt 作業ログ 2026-07-18

## 作業概要

`patch_study_gantt_20260718_default_md_sync.md` のパッチ仕様に従い、以下を実施。

1. **`DEFAULT_MD` をスケジュールv7に同期**（`public/schedule.md` と差分ゼロ）
2. **`APP_VERSION` 更新**（`20260716-1` → `20260718-1`）
3. **sw.js の `CACHE` バンプ**（v3 → v4）
4. 仕様書の変更履歴に1行追加

仕様変更なし。運用ルール（「スケジュール更新時は `DEFAULT_MD` も同内容に更新する」）に定められた同期作業＋版管理の定例作業のみ。

---

## 修正1: DEFAULT_MD のv7同期

- 原本 `public/schedule.md`（v7差し替え済み・コミット 4969ceb）を Node スクリプトで読み込み、`JSON.stringify` した文字列で `app.js` の `const DEFAULT_MD = ...;` 行を丸ごと置換。**手貼りではないため差分ゼロが機械的に保証される**（受け入れ条件）。
- 置換後、`DEFAULT_MD` を評価して原本と `===` 比較 → **identical: true** を確認。

## 修正2・3: 版管理

- `app.js`: `APP_VERSION = '20260718-1'`
- `sw.js`: `CACHE = 'study-gantt-v4'`

---

## 変更しなかったこと（パッチ仕様どおり）

- `public/schedule.md` そのもの（差し替え済み。触っていない）
- パースロジック・表示ロジック一切（v7はフォーマット上v6と同一構造。パーサ変更不要）
- 取得チェーンの構造・優先順位
- localStorage のキー・進捗データ（`sg-completed`）。タスク構成が変わった週のID移行処理は**書いていない**（未着手週のため実害なし、パッチ仕様の指示どおり）
- ファイル名（`public/schedule.md` のまま）

---

## 変更ファイル

| ファイル | 変更 |
|---------|------|
| `app.js` | `DEFAULT_MD` をv7内容に置換 / `APP_VERSION` → `20260718-1` |
| `sw.js` | `CACHE` v3→v4 |
| `docs/spec_study_gantt.md` | 変更履歴に「2026-07-18: DEFAULT_MD をスケジュールv7に同期、APP_VERSION/CACHE更新」を追加 |
| `docs/logs/` | 本ログ |

※ パッチ仕様の「4. README 更新」について: README.md はリポジトリに存在しない（20260716ログでも確認済み）。変更履歴は仕様書 `docs/spec_study_gantt.md` 内の変更履歴表に追記した。「実装判断で決めた値」の変更は無し（パッチの想定どおり）。

---

## 検証

- `node --check` で `app.js` / `sw.js` の構文OK。
- `DEFAULT_MD` と `public/schedule.md` の全文一致（`===`）を確認。
- 受け入れ基準3の目視ポイントに対応する内容が `DEFAULT_MD` 内に存在することを機械確認（全パス）:
  - B「Week 12: 定着① ネットワーク再走＋TW実地観察（2026-07-12 〜 2026-07-25）」
  - C「Week 13: 深掘り・外 — MCPサーバ実践（2026-07-26 〜 2026-08-15）」
  - 終了日 2026-08-29 / マイルストーン 8/15×2・8/16・8/29×2

---

## 注意点・申し送り

- **受け入れ確認はデプロイ（push）後**、パッチ仕様書の受け入れ基準1〜5をAKが端末で確認。2回リロードでフッター `app 20260718-1` を確認してから以降の項目へ。
- **本丸は基準5（フォールバック確認）**: 機内モード＋`sg-schedule-md` 削除で起動 → ⚠「内蔵フォールバック」表示の内容がv7であること。確認後は通常状態へ戻して基準2を再確認。
- 今後スケジュールmdを更新する際は、同じ手順（Node で `JSON.stringify` 置換）を使うと差分ゼロが保証できる。
